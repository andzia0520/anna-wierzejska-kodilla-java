AD PUNKT 1.

SELECT I.ID, I.SUMMARY, I.DESCRIPTION, I.USER_ID_ASSIGNEDTO, IL. NAME
FROM ISSUES I
JOIN ISSUESLISTS IL ON I.ISSUESLIST_ID = IL.ID;

LUB

SELECT I. *, IL. NAME
FROM ISSUES I
JOIN ISSUESLISTS IL ON I.ISSUESLIST_ID = IL.ID;



AD PUNKT 2.

SELECT I. *, U. FIRSTNAME, U.LASTNAME
FROM USERS U
JOIN ISSUES I ON U.ID = I.USER_ID_ASSIGNEDTO;


AD PUNKT 3.

SELECT U.FIRSTNAME, U.LASTNAME, COUNT(*) AS TASK_QUANTITY
FROM USERS U JOIN ISSUES I ON U.ID = I.USER_ID_ASSIGNEDTO
GROUP BY I.USER_ID_ASSIGNEDTO
HAVING COUNT(*) > 0;